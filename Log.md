# 11/04
- I have created a basic code for matrix multiplication with streaming `matmul.cpp`. It was GPT generated.
- I have a few doubts
  1. for the FIR assignment, sir had given IPs. Are they necessary here as well. do we need to make our own?
  2. we are using DMA right. how does vitis/vivado know that it has to place a DMA?
- I need to create a python file that will generate and send the matrices to `matmul.cpp` for *C simulation*. Will work on this tomorrow
- we need another script to interact with accelerator after flashing the bitstream onto the FPGA.